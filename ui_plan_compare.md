# Cocktail R UI 最终升级方案（逐页对比）

本文档给出 **Cocktail R** 本轮 UI 升级的最终方向。重点不是“做一些更好看的卡片”，而是把整个产品统一到 **Apple HIG / iOS 原生应用逻辑** 上：内容优先、导航清晰、层级克制、交互稳定。

当前我已经确认的最终原则如下：所有主要页面统一使用 **iOS grouped background**、**Inset Grouped 分组结构**、**SF Pro 字体层级**、**弱化装饰、强化信息结构** 的方式来重做。也就是说，这一轮不是局部修补，而是一次全应用的视觉和交互语言统一。

---

## 一、全局设计基线

| 模块 | 当前状态 | 最终方案 |
|---|---|---|
| 背景 | 偏白、页面与卡片层级不够明显 | 使用 `#F2F2F7` 作为 light 模式主背景，形成标准 iOS grouped 页面气质 |
| 卡片/分组 | 有些页面像普通白底列表，分组边界不够明确 | 统一为 **Inset Grouped**：白色内容组、10pt 左右圆角、组间留白 |
| 字体层级 | 大小和粗细不够统一 | 标题/分组标题/正文/辅助信息按 SF Pro 逻辑统一 |
| 按钮反馈 | 目前交互反馈风格不完全统一 | 统一为 **opacity press feedback**，减少夸张缩放 |
| 标签/筛选器 | 视觉样式较散 | 统一为 28pt 高度、13pt 字号、系统胶囊样式 |
| 删除/危险操作 | 部分页面仍偏工程化 | 统一改为 **Action Sheet / destructive** 逻辑 |

---

## 二、逐页最终方案

## 1. 酒单首页 / 配方列表页

从当前页面来看，这一页已经具备了较完整的信息密度，但问题是 **“像一个功能页”，还不够像 iPhone 上精修过的原生内容页”**。搜索、筛选、分段切换、卡片列表都在，但层级之间比较“平”，顶部和列表之间也缺少 Apple 式的呼吸感。

### 改前特征

| 观察项 | 当前表现 |
|---|---|
| 页面标题 | 已有标题与副标题，但与筛选区之间呼吸感不足 |
| 分段切换 | “酒单 / 研发 / 门店”已存在，但视觉更像普通按钮排布 |
| 搜索区 | 搜索和辅助按钮并排，稍显拥挤 |
| 筛选标签 | 功能完整，但标签视觉密度偏高，优先级不够清楚 |
| 配方卡片 | 信息较全，但当前更接近“数据列表”，不够 iOS 内容卡片化 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 顶部标题区 | 保留大标题“酒单”，副标题使用较轻字重，整体更接近 iOS Large Title 页面头部 |
| 分段切换 | 重做为更标准的 **segmented control** 视觉，选中态更明确，背景弱化 |
| 搜索栏 | 单独占一行，左右留白更大，右侧辅助按钮改成更轻的次级操作 |
| 筛选标签区 | 使用 28pt 胶囊标签，减少颜色数量，选中态以背景高亮为主 |
| 配方项 | 每项做成更标准的内容单元：名称、英文名、标签、摘要、右侧状态/收藏/选择更有秩序 |
| 页面节奏 | 列表项高度、标签间距、区块上下间距全部统一 |

### 视觉感受变化

> 改前是“功能很多的配方数据库页”；改后应当是“像 Apple 原生内容应用里的精致配方总览页”。

---

## 2. 酒库列表页

当前酒库页的优点是非常直接：搜索、分类、价格、容量都已经有了。但问题在于它更像后台库存表，而不是 iOS 中强调扫描效率和层级感的资料列表页。

### 改前特征

| 观察项 | 当前表现 |
|---|---|
| 顶部分段 | 功能清晰，但和页面背景融合度过高 |
| 标签筛选 | 已可用，但缺少更清楚的主次分层 |
| 列表行 | 品名、英文名、容量、价格都有，但布局略直白 |
| 价格区 | 右侧价格信息可见，但视觉重心稍硬 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 顶部分段 | “基酒库 / 酒款库 / 原材料库”采用 iOS 风格 segmented control |
| 搜索区 | 与筛选区拆分更清楚，降低拥挤感 |
| 分类标签 | 风格统一到系统胶囊标签；减少阴影和非必要强调 |
| 列表单元 | 左侧主信息更整洁，右侧价格做成 **辅助强调，不压过名称** |
| 价格呈现 | 使用更温和的强调色系与更好的纵向对齐，让“价格”易扫读但不过于商业化 |
| 行内信息 | 容量、分类、状态等采用更一致的小号文字与辅助色 |

### 视觉感受变化

> 改前更像酒库数据条目；改后应当更像 iOS 资料库应用中的“可快速扫描、可快速比较”的高质量列表。

---

## 3. 自制原料 / Homemade 列表页

这一页未来会是 Cocktail R 的特色页面之一，因为它不是普通的“原料库”，而是用户自己体系化管理 syrup、infusion、cordial、bitter、fat-wash、clarified juice 等自制准备物的地方。因此它不能只做成“另一个列表”，而要体现出 **结构感、工艺感、实验室感，但仍是 Apple 原生逻辑**。

### 改前特征

| 观察项 | 当前表现 |
|---|---|
| 页面结构 | 目前与其他库页风格接近，但还没有充分体现自制库的独特性 |
| 标签信息 | 已经有类型、工艺等数据能力基础 |
| 列表项 | 需要更明确地区分“类型 / 分区 / 工艺 / 用途” |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 顶部分段/筛选 | 与全局统一，同时保留 prep 类型和 section 的高效筛选 |
| 列表信息层级 | 名称第一层，类型/分区第二层，工艺/风味/用途作为第三层辅助信息 |
| 标签视觉 | 主标签与次标签的颜色、明度、字号统一，避免花哨 |
| 列表单元 | 增加更适合“专业自制库”的信息秩序，让用户一眼看出这是什么、怎么做、怎么用 |

### 视觉感受变化

> 改前是功能已经存在的原料列表；改后会更像专业酒吧 prep system 的移动端原生管理界面。

---

## 4. 配方表单页

配方表单是本次改造的核心页面之一。因为它不只是“输入表单”，还是 **AI 补全 + 人工校验 + 专业录入** 的混合工作流页面。现在它在功能上很强，但视觉结构还没有完全切换到 iOS 表单语言。

### 改前问题

| 观察项 | 当前问题 |
|---|---|
| 字段组织 | 更像工程表单拼接，而不是系统级表单分组 |
| 区块层级 | 有区块，但分组边界和节奏还不够 iOS |
| AI 按钮 | 功能强，但需要更像“高优先级工具动作”而不是普通按钮 |
| 字段可读性 | 字段名 / 输入值 / 辅助文案之间还可进一步统一 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 表单骨架 | 全面改成 **Inset Grouped List** 风格 |
| 字段分组 | 例如“基础信息 / 配方结构 / 风味与分类 / 来源信息 / AI 辅助”分组清楚 |
| 行样式 | 每行高度、文字大小、分割线逻辑统一，像 iOS 设置页与系统表单的结合 |
| AI 补全按钮 | 保留并强化，但以更克制、更高级的方式呈现；它是工具入口，不应显得廉价或花哨 |
| 固定标签字段 | 饮用时长、饮用场合、风味等继续使用固定预设，不做自由输入 |

### 视觉感受变化

> 改前是“功能很强的专业表单”；改后应当是“原生 iOS 风格的专业录入工作台”。

---

## 5. 酒库表单页

酒库表单需要体现的是“专业、快速、可信”。因为 bottle 的录入通常要更短、更直，用户希望尽快完成，同时又保留 AI 补全的辅助效率。

### 改前问题

| 观察项 | 当前问题 |
|---|---|
| 结构 | 表单结构可用，但不够原生 |
| AI 介入 | 已有 AI 补全逻辑，但视觉地位还可优化 |
| 附加信息 | distilleryInfo、pairingNotes、usageNotes 等新字段需要更清晰的组织方式 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 基础录入 | 名称、品牌、分类、容量、价格等放在最前并用标准 grouped rows 呈现 |
| AI 辅助区 | 作为单独分组存在，强调“先给你初稿，再由你判断” |
| 扩展字段 | 蒸馏厂信息、搭配建议、使用建议、季节性等放到更适合阅读的次级区块 |
| 表单节奏 | 让用户可以用最短路径完成录入，也可以展开做深度记录 |

---

## 6. 自制原料表单页

这个页面在 Apple HIG 逻辑下会比一般表单更重要，因为它承载了 **type / technique / flavor / usage** 这些高度结构化信息。它必须既像 iOS 表单，又要支持专业知识录入。

### 改前问题

| 观察项 | 当前问题 |
|---|---|
| 字段密度 | 新增字段较多，容易显得拥挤 |
| 信息层级 | story、styleDesc、usageNotes、flavorTags、techniques 需要更明确主次 |
| AI 入口 | 已加入 AI 补全，但需要更自然地并入工作流 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 基础字段 | section、type、名称、酒精度/容量等放在第一层 |
| 工艺字段 | technique 选择、风味标签、用途建议单独成组 |
| 文本字段 | story / styleDesc / usageNotes 作为阅读型内容区，避免与基础输入混在一起 |
| AI 补全 | 强化为“辅助起草”，并保留用户二次确认逻辑 |

### 视觉感受变化

> 改后这页会更像一个“prep dossier（自制档案）编辑器”，而不是普通备注表单。

---

## 7. 配方详情页

配方详情页不能再是“很多信息纵向堆叠”。Apple 风格的详情页必须做到：**顶部动作简单、内容区分组明确、关键信息先读到**。

### 改前问题

| 观察项 | 当前问题 |
|---|---|
| 操作按钮 | 若在内容区中出现太多操作，会破坏阅读节奏 |
| 内容组织 | 信息卡需要更强的组块逻辑 |
| 删除/编辑 | 应该更系统化，而不是页面内随意放置 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 顶部导航 | 右上角为 Edit，返回逻辑简单清楚 |
| 头部信息 | 配方名、英文名、分类标签、关键信息形成一个清晰头部卡组 |
| 内容区 | 配料、做法、故事、来源、AI 建议字段分组展示 |
| 删除 | 放到 Action Sheet 中，作为 destructive 操作 |

### 视觉感受变化

> 改后更像系统内容详情页，而不是表单数据结果页。

---

## 8. 酒款详情页

这一页已经加上了 category + style 标签，这是正确方向。下一步需要做的是让整页从“信息可见”升级成“信息有秩序”。

### 改前优势与不足

| 方面 | 当前情况 |
|---|---|
| 优势 | 已有名称下方标签行，信息基础不错 |
| 不足 | 页面整体还需要更完整的 iOS 分组阅读结构 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 头部 | 名称、英文名、分类、style 标签整合成更清晰的头部信息区 |
| 主体 | 品牌、产地、容量、价格、蒸馏厂信息、搭配说明、使用说明分组化 |
| 导航动作 | 编辑放导航栏，删除归入 Action Sheet |

---

## 9. 自制原料详情页

这一页已经具备 “section + type + technique” 标签基础，非常适合往 Apple 风格的专业详情页继续推进。

### 改前优势与不足

| 方面 | 当前情况 |
|---|---|
| 优势 | 标签表达已经比普通详情页更有专业结构 |
| 不足 | story / style / usage / technique 等还需要阅读顺序优化 |

### 改后目标

| 区域 | 最终方案 |
|---|---|
| 头部 | 名称、分区、类型、工艺标签形成专业头部 |
| 内容区 | 风味、故事、做法、用途、保存/应用建议等拆成更易读的信息组 |
| 操作 | 编辑在导航栏，删除走 Action Sheet |

### 视觉感受变化

> 改后会更像专业 bar lab 的 prep profile 页面。

---

## 三、你最关心的“对比”结论

如果一句话概括这次最终方案：

> **改前：功能很强，但页面之间风格还像逐步长出来的工具集合。**
>
> **改后：所有页面都会收敛到统一的 iOS 原生设计语言，像一个完整产品，而不是多个功能页拼接。**

具体来说，对比主要体现在下面几项：

| 维度 | 改前 | 改后 |
|---|---|---|
| 视觉气质 | 工具型、偏工程 | 原生、克制、专业 |
| 分组方式 | 局部有组块，但不完全统一 | 全部统一为 grouped / inset grouped 逻辑 |
| 顶部结构 | 每页各自略有差异 | 统一 Large Title / segmented / search / filter 节奏 |
| 列表体验 | 信息密度高但层级略平 | 更适合快速扫描和快速判断 |
| 表单体验 | 功能强但略像后台编辑器 | 更像 iOS 上的专业录入页 |
| 详情体验 | 信息存在但阅读节奏仍可增强 | 更像内容详情而不是数据输出 |
| 危险操作 | 工程感偏重 | 全面收敛到 Action Sheet / destructive 语义 |

---

## 四、当前执行状态

目前这份文档描述的是 **最终实施方案**。也就是说，这是我接下来实际改造每个页面时会遵循的统一标准。

下一步执行顺序将是：

1. 先做全局主题与视觉 token 统一；
2. 再改三大列表页；
3. 再改三个表单页；
4. 最后改三个详情页与危险操作交互；
5. 完成后验证 TypeScript 零错误、保存版本，再准备 TestFlight 提交流程。

如果你要看“非常直观的版本”，下一轮我就可以直接给你产出：

- **逐页改造后的真实截图**；
- 以及一份 **改前 / 改后对照图集**。

