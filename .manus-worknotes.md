# Build 55 修复工作笔记（2026-07-19）—— 沙盒重置后重建

## 【沙盒重置事件 14:30】
- 沙盒被重置回 checkpoint 34c82e3，之前所有未保存的客户端修改全部丢失，需要重做
- vitest.config.ts 幸存（重置后我新建的）
- **Worker 端修复不受影响**（部署在 Cloudflare，Version 63df4c0c-973c-4349-abd6-2b3ae617ffcd，debug 端点已移除，E2E 30/30 PASS 两次）

## Worker 端已完成（无需重做）
- initDB 自动建表+ALTER 迁移（补 owner_device_id/device_id/token/platform/is_active 列，建 sync_tombstones/kv_cache/balance_history/ai_usage_log 表）
- register/pair 尊重客户端 deviceId、同时返回 token+deviceToken
- pull 返回驼峰 {storageKey, value, clientUpdatedAt}
- list 返回映射好的 {id, name, role, allowedKeys, last_seen, created_at, isCurrentDevice}
- verifyDevice d.id → d.device_id 修复
- balance 响应加 checkedAt
- E2E 验证 30/30 全部通过（含双设备互通、配对码防重用、坏token拒绝）
- 余额实测 9.68 CNY；/health OK；AI 路由 400（正常，非500）
- Worker 备份: 云端PC ~/cf-worker-deploy/worker.js.bak-build55

## 客户端修改（沙盒重置丢失，需重做）
1. **lib/cf-sync/provider.tsx**：
   - performSync 提取（register→pull→runInitialSync→push），暴露 retrySync + syncError 到 context
   - 自动重试：指数退避 30s*2^n 最大10分钟、最多8次；logout 清 syncError
   - "iCloud Drive auto-backup" 注释改为 "local-documents auto-backup"
2. **app/device-manager.tsx**：
   - line ~71: `/api/balance/check` → `/api/balance`
   - useSync() 解构加 retrySync, syncError
   - manualSyncing state + handleSyncNow（Alert 错误/成功haptic+刷新）
   - syncRow 内加"立即同步/Sync Now"按钮（primary 圆角，isSyncing 时 disabled+ActivityIndicator）
   - combinedError = syncState.error || syncError 用于颜色/标签/错误行
   - iCloud 通道 label 改"本机文档备份/Local Documents"，detail 加"随iCloud整机备份"
3. **app/recipe/[id].tsx** handlePickPhoto（line ~82-130）：
   - import * as ImageManipulator from "expo-image-manipulator"
   - 相册权限：iOS PHPicker 无需权限，仅 Android && !canAskAgain 时 Alert
   - 压缩：manipulateAsync(asset.uri, width>1600?[{resize:{width:1600}}]:[], {compress:0.8, format:JPEG})，失败 fallback 原图
   - destPath 统一 `.jpg`；catch 改 Alert.alert(t("detail.photo.error.title"), t("detail.photo.error.msg")+msg)
4. **lib/i18n/translations.ts**：加 detail.photo.error.title/msg 两个键（在 detail.photo.perm.cancel 后）
5. **lib/recipes/photo.ts** persistPhoto：ext 用正则 /\.([a-zA-Z0-9]{2,5})(?:\?.*)?$/ 从最后段提取，无匹配回退 jpg
6. **package.json**：expo-image-manipulator ~14.0.8（需重新 npx expo install expo-image-manipulator）
7. **todo.md**：Build 55 修复项（重置丢失，需重加）
8. vitest.config.ts 已存在（alias @ → 根目录，19 tests passed）

## Cloudflare 信息
- Worker: cocktail-ai → https://cocktail-ai.kikikong2017.workers.dev（Version 63df4c0c）
- D1: cocktail-r-db (1fe2ef35-4bcd-42b7-a872-bc93d03593c6)
- CF Token (Workers+D1): cfut_1YCQlb1uK1Zil8Df8HhDMH8ulL9pfFL7DXgkIgYB0b44e0de
- 部署: cd ~/cf-worker-deploy && CLOUDFLARE_API_TOKEN=... wrangler deploy --no-bundle

## EAS 信息
- EXPO_TOKEN: x_5a1Tebvrgg5bHFMcdBmkjHj5IutY-4zBRfzpHs
- 云端PC ~/cocktail-r；Build 流程: checkpoint → git pull → eas build --platform ios --profile production --non-interactive --no-wait
- Submit: eas submit --platform ios --profile production --latest --non-interactive
- 上个 build #37 = appBuildVersion 54；Apple App ID 6788653669

## 用户要求
- 修复完成后先二次彻底检查并汇报，**不触发 Build**，等确认

## 2026-07-19 视觉升级验证
- Web 预览注入配方 preview-negroni-1（localStorage: cocktail.recipes/cocktail.seeded/cocktail.bottles）
- 验证结果：第一排「经典(金色)/高球ⓘ(灰)/慢饮/餐前」，第二排「苦韵/草本/柑橘」独立成排 ✓
- 配料区去蓝：添加利金酒/金巴利/香料甜味美思浸渍 全部黑字+灰箭头，浸渍 ✦ 淡紫 ✓
- 成本区去蓝：明细行黑字，总价 ¥12.2 蓝色保留（白名单），联网补全按钮蓝色保留 ✓
- 变体归属「经典原方 Classic」：绿色book图标+绿色文字（经典状态保留绿色）+灰箭头 ✓
- CodexFamilyBadge：灰边框+surface背景+深灰semibold+灰ⓘ13px ✓
- 收藏星蓝色保留 ✓
- tsc 0 错误，vitest 19/19 通过
- webdev_take_screenshot 与浏览器上下文不共享 localStorage，须用 browser_* 工具截图注入数据后的效果
- 截图路径：/home/ubuntu/screenshots/8081-ihxwxkmzmvhv2kd_2026-07-19_15-13-18_5050.webp

## 2026-07-19 book-reader 审查（用户报告：Mac 上翻页失效+频闪、界面显示问题、单双页切换、对标 iOS 图书 App）
用户环境：Mac 上通过「我的 Mac (Designed for iPad)」或 Catalyst 方式运行 iOS App（TestFlight），大屏横向窗口。
截图观察：
- 截图1: 阅读器页面宽度撑满 Mac 宽屏，正文单栏横向过宽（一行文字极长），底部进度条+章节页码
- 截图2/3/4: 用户展示 Apple Books 的双页布局（图左文右）、目录 popover、章节页码「41/171页 本章最后一页」等体验
- 截图5: Apple Books 侧边目录 drawer（Ch. 23 of 129）

### 代码审查发现（app/book-reader.tsx, 2468 行）
1. **翻页失效（Mac）根因候选**：
   - pageFlipGesture 使用 Gesture.Pan（触摸手势）。Mac (Designed for iPad/Catalyst) 用鼠标操作，Pan 手势对鼠标点击拖动支持差；且没有点击左右边缘翻页的热区，也没有键盘方向键支持 → Mac 上几乎无法翻页
   - 底部左右箭头按钮存在（sed 1560+ 待确认），但 chrome 4 秒自动隐藏
2. **频闪根因候选**：
   - HtmlChapter 的 fullHtml useMemo 依赖 [html, css, fontSize, lineHeight, bgColor, textColor, linkColor, pageFlipMode, fontFamilyCss, letterSpacingCss, marginPx] — WebView source={{html}} 每次 fullHtml 变化就整页 reload（白屏闪烁）
   - highlights 注入 useEffect 依赖 [chapterHtml, chapterIdx, highlights]，700ms 后 injectJavaScript；如果 highlights 数组引用频繁变化会反复注入
   - 进度条 PanResponder onPanResponderMove 连续 setChapterIdx → 连续触发章节加载 effect（setLoadingChapter(true)→setChapterHtml(null)→异步读文件）→ 反复 loading 闪烁（自动频闪的最可能根因！拖动进度条时每移动一像素都可能换章节，每次都 loading + WebView 重建）
   - autoSave setInterval 每 30s updatePosition → books store 更新 → book 对象引用变化 → 章节加载 effect 依赖 [book, chapterIdx, loadChapter]，book 引用变了就 setLoadingChapter(true)+setChapterHtml(null) 整章重载 → **周期性频闪的确凿根因**（每30秒白屏一次）
   - 同理 updatePosition(chapterIdx 变化时) 也触发 book 引用变化 → 双重加载
3. **单双页显示**：目前只有单页 WebView columns 布局（columns:1, column-width: calc(100vw - margin*2)）。无宽屏双页（two-up）支持。Mac/iPad 横屏宽度 >900px 应显示双栏对页（Apple Books 行为）
4. **界面显示问题**：
   - web 平台直接 dangerouslySetInnerHTML 渲染 fullHtml（含 <html> 全文档标签嵌入 div，非法 DOM）
   - 宽屏无 max-width 限制，行长极长（截图1 正文一行 ~200 字符，可读性差）；Apple Books 限制约 38em
   - 底部 80px padding 固定；页脚页码与进度条重叠遮挡
5. **性能/流畅度**：
   - cacheEnabled={false} 每次重载都重新解析；inlineImagesAsBase64 每章同步 base64 内联全部图片（大图章节慢）
   - injectedJavaScript 在 source 变化后需重新注入，__pf_injected 防重标志在 reload 后自然重置（OK）
   - swipeAnimStyle 只在 totalPages<=1 时生效，多页章节无拖拽动画（无跟手翻页效果，与 Apple Books 差距大）
6. **其他潜在 bug**：
   - goNextPage/goPrevPage 依赖 [goNextChapter] 但读取 currentPageRef/totalPagesRef（ref 同步 OK），但 setCurrentPage 后 currentPageInChapter 未同步更新
   - onPageInfo 回调 setCurrentPageInChapter(0) 但 currentPage 未重置（chapter load effect 里 setCurrentPage(0) 有做）
   - handleInternalLink 跳章后 TODO: 未滚动到 anchor
   - progressPanResponder deps [book] — book 引用每30s变化导致 PanResponder 重建
   - 书签 bookmarks 仅存于 state，未持久化到 AsyncStorage（退出丢失）
   - 阅读位置保存：updatePosition(book.id, chapterIdx, chapterIdx, currentPageRef.current) 参数疑似 (id, lastChapter, lastChapter, lastPage)，需核对签名
   - Web 平台 HtmlChapter 无翻页模式（scroll only）、无点击事件（onTap undefined）→ Mac Web 预览无 chrome 唤出问题

### 频闪根因确认（已核实代码）
- updatePosition 每次 setBooks(prev.map(...)) → 目标 book 对象新引用
- book-reader L538: `const book = books.find(...)` → book 引用变化
- L719 章节加载 effect deps [book, chapterIdx, loadChapter] → book 引用变 → setLoadingChapter(true)+setChapterHtml(null) → WebView 卸载重建 → 白屏闪烁
- 触发链1：30s autoSave → updatePosition → 闪烁（周期性"自动频闪"，与用户描述吻合）
- 触发链2：换章 effect L781 updatePosition → book 引用变 → 章节加载 effect 再跑一次（双重加载）
- 触发链3：L785 updateBook(readingStatus) 首次打开时又一次 → 三重加载
- 修复：章节加载 effect 依赖改为 [book?.id, chapterIdx]（或用 ref 比较），loadChapter 用 ref
### 翻页失效（Mac）确认
- 唯一的页级翻页入口：Pan 手势（触摸）；底部按钮只切章不翻页；无点击边缘翻页热区、无键盘支持
- Mac (Designed for iPad) 鼠标拖动可能被 WebView 内部文本选择拦截，Pan 手势难以触发
- 修复方案：a) 点击左右 25% 屏幕区域翻页（Apple Books 行为）b) 键盘左右方向键 c) 底部按钮在 pageFlipMode 时改为翻页而非切章
### 单双页
- 现仅单栏。Apple Books 在宽度足够（横屏/Mac）时自动双页。方案：window.innerWidth>=900 时 columns:2 + column-gap，goToPage 步长 = innerWidth，页数计算不变（scrollWidth/innerWidth）

### Apple Books 对标要点（研究结论）
1. 布局：横屏/宽屏自动双页对开（two-up spread），竖屏单页；页码「41/171页 · 本章最后一页」在页脚居中
2. 翻页交互：a) 左右边缘点击 b) 拖拽跟手 page-curl/slide 动画 c) 键盘方向键（Mac/iPad+键盘）
3. 分页技术共识（Readium）：CSS columns 是行业标准做法（我们已用）；WebView 单资源+原生 chrome 是推荐架构（我们已用）；平滑过渡需要"快照+相邻页预载"或双 WebView
4. 章节页码：Apple Books 显示全书页码 + 本章剩余；我们只显示章内页码
5. 行长控制：正文最大行宽约 38-42em；宽屏双栏每栏保持舒适行长
6. 目录：侧边 drawer/popover，显示章节+页码，当前章高亮（我们已有 TOC modal）
7. 设置：字体、字号、行距、背景主题（白/深/棕褐）——我们已有；Apple 另有「滚动视图」模式切换

## 2026-07-19 第二轮：用户追问（严谨性复审 + Apple Books 工具栏 UI）
用户新需求：
1. 评估原方案是否彻底、有无潜在 bug；有无更专业的渲染方案
2. 完全借鉴 Apple 图书 UI 更新上下功能排（chrome）
3. 修复「功能排出现会让阅读界面缩小/移动」的 bug
4. 继续深扒图书 App，给严谨升级方案，等确认

### 工具栏挤压根因（已核实代码）
- styles.topBar 是普通 flex 布局（无 position:absolute），条件渲染 {chromeVisible && <View topBar>}
- → chrome 出现时 topBar 插入布局流，把下面的阅读区 View 往下推、高度变小 → 「界面缩小/移动」
- WebView 高度变化还会触发 resize → 列重排 → 当前页跳动（双重伤害：位置也丢）
- bottomBar/extractBar/footer 已是 absolute（不推挤），只有 topBar 是流内元素
- 修复：topBar 改 position:absolute top:0 + 半透明背景 + fade 动画，阅读区永远满屏（Apple Books 即 overlay 模式）
- 注意：ScreenContainer edges=["top"] 的 SafeArea padding 也要处理——topBar overlay 需覆盖到状态栏区域，阅读区改为 edges=[] 自己管理 insets

### Apple Books chrome 设计（用户截图分析）
- 顶栏：左侧红绿灯+目录/外观/笔记图标（Mac），中间书名，右侧「大小(AA)/搜索/书签」
- 顶栏是 overlay：出现/消失不影响正文位置，半透明毛玻璃
- 页脚：极简文本「8/171页」居中 +「本章最后一页」右下角，常驻（非 chrome），字号约11-12px 灰色
- 目录：popover 锚定在目录按钮下方（Mac），列表含章节层级缩进+页码，当前章高亮灰底
- 无底部工具栏！翻页箭头 ‹ › 悬浮在左右边缘垂直居中，半透明，hover 显示（Mac）
- 进度：Apple Books 用页码文本而非进度条滑块（新版）；滑块在 iOS 底部（拖动时显示预览气泡）

### 原方案严谨性复审（自查结论）
1. 频闪修复（依赖改 book?.id）：彻底性 OK，但要注意 loadChapter 也在 deps；book 内容真正变化（如重导入）时需刷新 → 方案：依赖 [book?.id, chapterIdx, book?.sections?.length]
2. 潜在坑：goToPage 用 scrollLeft 跳页在 iOS WKWebView 上 columns 布局可能有亚像素误差累积 → 更稳的做法：translateX(-pageIdx*100vw) transform 移动 body 内容器，或 scrollTo({left, behavior:'smooth'})；transform 方案还自带 GPU 加速动画（专业做法，Readium 用 transform）
3. 点击热区方案潜在冲突：WebView 内部 click 事件 vs RN 层 Pressable 热区。若 RN 层盖住 WebView 左右 25%，文本选择/链接点击会被挡 → 专业做法：在 WebView 注入 JS 判断 click x 坐标（<25% prev, >75% next, 中间 tap），通过 postMessage 通知 RN，无 RN 层遮挡
4. 键盘方向键：WebView 需 focus 才收到 keydown；Mac Designed for iPad 上 RN 层可用 useEffect + KeyboardEvent（react-native-web 不适用）→ 稳妥做法：WebView 内 document.addEventListener('keydown') + RN 侧 iOS 原生 UIKeyCommand 不可用（Expo），所以 WebView 内监听是唯一路径，WebView 默认有焦点
5. 双页方案：columns:2 时 column-width 需 calc((100vw - gap - 2*margin)/2)；跳页步长仍 innerWidth；重排后保持位置：记录当前页首元素/文本 offset，resize 后 scrollIntoView——专业做法是用 CFI (EPUB Canonical Fragment Identifier)，简化版可用「章内字符偏移比例」恢复位置
6. 更专业的渲染替代方案评估：
   a) @epubjs-react-native (epub.js): 成熟分页/CFI/主题，但 epub.js 性能一般、维护弱，且我们书籍已解压为自有格式，迁移成本高 → 不建议
   b) Readium Mobile (react-native-readium): 专业级，但需原生模块（Expo prebuild），EAS 可用但复杂度高 → 中期可选
   c) 自研现架构优化（WebView+columns+transform 翻页+预载）：与 Apple Books 同类技术路线，改造成本最低 → 推荐
7. 预载方案细化：双 WebView（current + next 隐藏预热）内存翻倍但跨章即时；轻量版：预读 HTML 字符串缓存（Map<chapterIdx,html>，LRU 3章），WebView 重载仍有 ~200ms 白屏 → 用「切章时保留旧 WebView 截图覆盖直到新章 onLoadEnd」消除白屏（Readium rasterize 方案）
8. 高亮 700ms 定时 → onLoadEnd 驱动 + injectedJavaScriptBeforeContentLoaded 提前挂 __applyHighlights 定义

### 升级方案 v2 要点（待写入正式文档）
- A. 渲染核心改造：transform 翻页 + onLoadEnd 事件驱动 + HTML LRU 预载 + 截图防白屏 + cacheEnabled
- B. chrome 重构（Apple Books 风格）：topBar absolute overlay 毛玻璃(BlurView) fade in/out；去掉底部大工具栏；页脚常驻极简页码「x/y页 · 本章最后一页」；左右悬浮翻页箭头（Mac 常显半透明、iOS 隐藏）；TOC 改侧边 drawer（宽屏）/底部 sheet（窄屏）+ 当前章高亮 + 页码列
- C. 交互：WebView 内点击坐标分区（25/50/25）postMessage；keydown 监听（←→空格PgUp/PgDn）；进度滑块拖动防抖+预览气泡，松手跳转
- D. 布局：≥900px 双页 columns:2 + 42em 行长限制 + 位置恢复（字符偏移比例）
- E. Bug 修复：频闪三链、书签持久化、currentPage 状态合并、lastPage 恢复、progressPanResponder 依赖

### 第二轮深挖研究结论（Apple Books iOS16+ / Readium 工程规范）
1. iOS 16+ Apple Books 取消了顶部工具栏！控件收进「阅读菜单」按钮（默认右下角浮动圆钮），菜单含：目录、搜索、主题与设置、共享、锁定方向、书签
2. 点击屏幕唤出的是轻量 overlay 信息：顶部「本章剩余页数」气泡 + 底部「第 x / y 页」气泡，叠加在正文上、正文完全不动（100% overlay 设计）
3. 双击屏幕 = 加/去书签；跳转（目录/书签/滑块）后左上角出现「返回上一位置」按钮（类似浏览器 back/forward，带页码）
4. 右上角常驻 X 按钮 = 退出阅读器
5. 主题设置面板：顶部 [A- A+] [滚动/翻页切换] [亮/暗/跟随设备/跟随环境]；6 个预设主题；Options 里字体/加粗/行距/字距/词距/两端对齐/多栏
6. 翻页动画 iOS16 默认 slide（fast fade/curl/scroll 可选）；Mac 版保留顶栏（红绿灯窗口特性），所以 Mac 风格 = 顶栏 overlay + 左右悬浮箭头
7. Readium Web 官方规范：预载仅限相邻（前后）资源，主资源就绪后高优先级预取；虚拟化分页；注意 DOM 节点数/内存
8. 结论：我们的 chrome 设计应做「双态」——窄屏(iPhone)用 iOS16 风格浮动阅读菜单钮+气泡页码；宽屏(Mac/iPad横屏)用 Mac 风格顶栏 overlay + 悬浮翻页箭头。页码页脚常驻极简文本。所有 chrome 均 absolute overlay + fade，绝不推挤正文

### 实施阶段：book-reader.tsx 结构地图（2026-07-19 重读）
- L1-46 imports（已有 Reanimated/Gesture/AsyncStorage/WebView）
- L49-70 ExtractedRecipe 类型
- L76-122 resolveRelativePath + inlineImagesAsBase64（图片 base64 内联）
- L124-218 READER_CSS
- L222-241 ImageViewerModal
- L245-492 HtmlChapter 组件：
  - props: html/css/fontSize/lineHeight/theme/onTap/extractMode/onSelection/webViewRef/baseUrl/pageFlipMode/onPageInfo/onImageTap/onInternalLink/fontFamily/margin/letterSpacing/highlights/onHighlight
  - L283-290 fullHtml useMemo：pageFlipMode 用 columns:1 布局，padding 80px bottom
  - L301-391 combinedScript：selectionchange/click(imageTap/internalLink/tap)/长按高亮/__applyHighlights
  - L394-436 pageFlipScript：calcPages/sendPageInfo(load+DOMContentLoaded+resize)/__goToPage(scrollLeft)/__getCurrentPage/onScroll→pageScroll
  - L438-446 web 平台 dangerouslySetInnerHTML
  - L449-491 WebView：cacheEnabled=false, scrollEnabled=!pageFlipMode, onMessage 路由
- L522+ BookReaderScreen：
  - L537-538 useBookStore books.find
  - L569-586 章节状态+设置 state（SETTINGS_KEY/HIGHLIGHTS_KEY 每书独立）
  - L589-594 pageFlipMode(默认true) + swipeTranslateX 动画
  - L597-603 currentPage/currentPageRef/totalPagesRef/totalPages/currentPageInChapter（状态重叠）
  - L607-610 chromeVisible + navHistory
  - L613-618 提取模式 phase/blocks 等
- 之前审查已知：
  - 章节加载 effect 在 ~L660-700 区域，依赖 [book, chapterIdx, loadChapter] ← 频闪根因
  - 30s 自动保存 updatePosition 在 ~L700-730
  - Pan 手势翻页 ~L794-990（goNextPage/goPrevPage 用 injectJavaScript __goToPage）
  - render: topBar(L1343 流内!) → 阅读区 → bottomBar(absolute) → settingsSheet/extractBar/footer(absolute)
  - styles at L2333+：topBar 无 position:absolute
- HtmlChapter 使用处大约 L1430-1470（native）与 web 分支

### 实施细节决定
- READER_CSS 不动；改 pageFlipMode fullHtml：加 #__reader_content 包裹 body 内容? → 更简单：body 仍用 columns，翻页改为对 html/body 施加 transform：不行，columns 在 body 上。做法：body 保持 columns，新建 window.__goToPage 用 body.style.transform = translateX(-idx*100vw)，同时 html/body overflow hidden 已设置。注意 padding 影响：translateX 按 innerWidth 步长即可（column-width + gap = innerWidth）
- 双页：>=900px 时 column-count:2，column-gap 2*margin，页步长仍 innerWidth
- slide 动画：body transition: transform 260ms ease-out（iOS Books slide 规格），拖拽跟手时临时关 transition
- 键盘/点击分区都进 pageFlipScript
- 位置锚定：__getCharOffset/__restoreCharOffset：取当前页第一个可见文本节点在全文的字符偏移比例

### 补充结构（1300-2470 实读确认）
- L1338 return <ScreenContainer edges top/left/right>
- L1341-1389 topBar（流内! chromeVisible && phase==='reading'）: navHistory 返回 / 返回+页码、书名、设置/书签/TOC 按钮。styles.topBar 无 absolute（L2334-2341）
- L1392-1419 select/confirm 阶段各自 topBar（流内，这两个阶段可保留流内，不影响阅读）
- L1421-1497 阅读区 View flex:1 overflow hidden → GestureDetector → Animated.View(swipeAnimStyle) → HtmlChapter
  - onPageInfo L1452: setTotalPages + totalPagesRef + setCurrentPageInChapter(0)
  - baseUrl 计算 L1455-1482
- L1679-1743 bottomBar（absolute）: 进度条(PanResponder grant+move 都 setChapterIdx!) + 页信息文本 + 章节导航 ‹ › + AI 选区提取按钮
- L1746-1804 extractBar（absolute）
- L1806-1926 设置面板（absolute overlay + settingsSheet）：字体大小(重复两处 L1818-1831 与 L1893-1906)、行距、主题、字体、页边距、字间距、翻页模式开关
- L1928-1970 TOC drawer（absolute，右侧 72% 宽）：点击章节 setChapterIdx+关闭
- L2333-2454 styles：topBar 需加 position absolute top:0 left/right:0 zIndex；bottomBar/extractBar/footer 已 absolute
- L2455-2468 WEBVIEW_HEIGHT_SCRIPT（旧遗留，未见引用?）
- 书签目前是 local state bookmarks:number[]（L579），未持久化 → 用 store.updateBook({bookmarks}) 持久化（StoredBook 已有 bookmarks?: number[] 字段）
- store: updatePosition 每次 map 新 books 数组 → book 引用变 → L719 effect [book,...] 重跑 = 频闪链
- 章节加载 effect L719-778 依赖 [book, chapterIdx, loadChapter]
- L781-789 chapterIdx 变化时 updatePosition + readingStatus
- 手势 L858-901：totalPages<=1 时拖 outer；结束时页级/章级导航
- goNextPage/goPrevPage L834-856 使用 __goToPage
- handleInternalLink L794-817：跨章不推 navHistory（bug：navHistory 只在?未见 push 处，需搜索）
- ScreenContainer edges 含 top → topBar overlay 化后阅读区顶到 safe top 即可，chrome overlay 需加 insets.top padding
- BUG确认：navHistory 只有 pop（L1348），从无 push——内链跳转/TOC跳转都不推历史，「返回」按钮永远不出现 → P2 需在 handleInternalLink 跨章分支 & TOC 跳转 & 进度条跳转时 push

### 实施前最终结构确认（2026-07-19 第三轮实读）
- imports L2-45：已有 WebView/Gesture/Animated/AsyncStorage/useSafeAreaInsets/useWindowDimensions/PanResponder。无 BlurView（用半透明 bg 即可，避免新依赖）
- HtmlChapter L245-492：
  - props 完整列表见 L246-269；bgColor/textColor/linkColor L271-273
  - fullHtml useMemo L283-290：pageFlipMode 分支单栏 columns:1 + column-width calc(100vw-2m)；padding 80px bottom
  - combinedScript L301-391（__rn_injected 防重）：selectionchange/click(img/a/tap)/长按高亮/__applyHighlights
  - pageFlipScript L394-436（__pf_injected）：calcPages/sendPageInfo/__goToPage(scrollLeft)/__getCurrentPage/onScroll→pageScroll
  - web 分支 L438-446 dangerouslySetInnerHTML(fullHtml 全文档 — 非法DOM bug)
  - WebView L449-491：cacheEnabled=false、scrollEnabled=!pageFlipMode、onMessage 路由（pageScroll 目前 no-op！）、onShouldStartLoadWithRequest 白名单
- 状态区 L522-618：
  - L537-538 useBookStore books.find
  - L569 chapterIdx 初值 book?.lastChapter；L572 tocOpen
  - L575-586 fontSize16/lineHeight1.75/theme/showReaderSettings/bookmarks(state)/fontFamily/margin20/letterSpacing/highlights/highlightMenu；SETTINGS_KEY/HIGHLIGHTS_KEY 每书独立
  - L589 pageFlipMode(true)；L590 screenWidth；L591-594 swipeTranslateX/swipeAnimStyle
  - L597-602 currentPage/currentPageRef/totalPagesRef/totalPages/currentPageInChapter（三重状态需合并）
  - L607-610 chromeVisible/chromeTimer/navHistory
- 30s autoSave L638-647 deps [book, chapterIdx, phase, updatePosition]
- settings load L650-668 / persist L671-674 / highlights persist L677-680 / 高亮注入 L683-690（700ms timer，deps [chapterHtml, chapterIdx, highlights]）
- progressPanResponder L694-712（grant+move 都 setChapterIdx，deps [book] ← 30s重建）
- 章节加载 effect L719-778 deps [book, chapterIdx, loadChapter]
- 章变持久化 L781-789 deps [chapterIdx]
- handleInternalLink L794-817；goNext/PrevChapter L819-831；goNext/PrevPage L834-856（injectJavaScript __goToPage）
- pageFlipGesture L858-901；showChrome L903-910（4s auto hide）；handleTap L912-915
- ImageViewerModal L221-241；inlineImagesAsBase64 L90-122（全部图片同步 base64）

### 实施设计定稿（P0-P3 一体化改造）
1. 频闪：章节加载 effect deps 改 [book?.id, chapterIdx, book?.sections?.length]，loadChapter/book 用 ref（bookRef.current=book）；30s autoSave 保持但不影响渲染；progressPanResponder deps 改 [book?.id, book?.sections?.length]；进度条 grant/move 只 setScrubPreview（本地 state 气泡预览），release 才 setChapterIdx
2. topBar overlay：styles.topBar 加 position absolute top0 left0 right0 zIndex20 + paddingTop insets.top；ScreenContainer edges 改 ["left","right"]（top 由 chrome 自己管理），阅读区满屏；chrome fade 用 Animated opacity（reanimated withTiming 200ms）+ pointerEvents
3. 常驻页脚：极简文本「第 x / y 页 · 本章最后一页(当 x==y)」absolute bottom insets.bottom 居中 11px 灰色，zIndex10（chrome 之下）；点击屏幕唤出的 bottomBar 保持 absolute，但去掉大按钮排 → Apple 风格：进度滑块 + 章节标题 + AI 提取按钮保留（业务需要）
4. transform 翻页引擎（pageFlipScript v2）：
   - body 保持 columns；新增 __setPage(idx, animate)：body.style.transition = animate?'transform 260ms cubic-bezier(0.25,0.1,0.25,1)':'none'; body.style.transform='translateX(-'+idx*stepW+'px)'
   - stepW = window.innerWidth（columns 布局下每页宽 = 视口宽）
   - html,body overflow hidden 已设；弃用 scrollLeft
   - 点击分区：click handler 里 x<w*0.25→postMessage('pagePrev')；x>w*0.75→'pageNext'；中间→'tap'（img/a 优先判断保持）
   - keydown：ArrowLeft/PageUp→pagePrev；ArrowRight/PageDown/Space→pageNext
   - calcPages：Math.max(1, Math.ceil(sw/stepW))；resize 时重算并 __setPage(min(cur,total-1),false) + 位置恢复
   - 双页：innerWidth>=900 → body column-count:2（column-width auto）+ max-width none；单页时正文列宽限制 max 42em：column-width: min(calc(100vw-2m), 42em)？——columns 布局中 column-width 是提示值，安全做法：单页 >=680px 视口时加 padding 让内容列居中（BODY_PAD 动态计算）。简化：单页时 column-width calc(100vw-2m)，但在 wide 视口用更大水平 padding: max(m, (100vw - 42em)/2)
   - 字符偏移锚定：__getAnchor() 返回当前页第一个可见文本节点全文偏移比例；__restoreAnchor(ratio) 二分查找页
5. RN 侧：onMessage 加 pagePrev/pageNext/keyNav 路由 → goPrevPage/goNextPage；goToPage 改 __setPage(idx,true)；手势拖拽跟手：onUpdate 时 injectJavaScript 设置临时 transform（性能：节流 16ms）——简化：保留现有手势阈值翻页（onEnd 触发 __setPage 动画），拖拽跟手 P3 可选不做（WebView 内 touchmove 已可原生拖动? columns overflow hidden 无滚动）。决定：手势 onEnd 翻页 + __setPage 动画即可（slide 效果已有）
6. 悬浮箭头（宽屏常显）：screenWidth>=700 时左右垂直居中 ‹ › 半透明圆钮（absolute，zIndex15，不随 chrome 隐藏），点击 goPrev/NextPage
7. navHistory push：handleInternalLink 跨章分支、TOC 跳转、进度条 release 跳转前 push {chapterIdx, page:currentPageRef.current}；「返回」按钮进 topBar（已有 UI）+ 宽屏时也放浮动胶囊左上（简化：topBar 已有，仅补 push）
8. 书签持久化：bookmarks 初值 book?.bookmarks ?? []，toggle 时 updateBook(book.id,{bookmarks:next})
9. 预载：chapterCache = useRef(Map<number,string>) LRU5；章节加载 effect 先查缓存（同步 setChapterHtml，无 loading 闪）；加载完成后 idle 预取 chapterIdx±1 写缓存；换章白屏消除：loadingChapter 时不卸载 WebView → 改为 chapterHtml 保留旧值直到新值就绪（去掉 setChapterHtml(null)），加 fade 过渡（opacity 0.999 触发?）——做法:换章时保留旧 html，新 html 就绪直接替换 source（WebView reload 一次，配合缓存几乎即时）
10. currentPage 状态合并：删 currentPageInChapter，统一 currentPage + currentPageRef；onMessage pageScroll → setCurrentPage（transform 模式下无 scroll 事件，由 __setPage 后 postMessage pageChanged 回报）
11. lastPage 恢复：章节加载完成 onPageInfo 后若是初次进入（restorePageRef.current 非 null）__setPage(min(lastPage,total-1),false)
12. web 分支修复：只嵌入 body 内容 + scoped style（去 html/head），max-width 42em margin auto
13. 设置面板：去重复字号块（删 L1818-1831 第一个）；翻页模式开关文案不变；新增「双页对开」segmented：自动/单页/双页（autoTwoUp state，默认 auto>=900px）
14. 高亮注入 timer 700→由 onLoadEnd 驱动（onMessage 'ready' 事件，pageFlipScript sendPageInfo 时同时标记 ready）

## 实施进度快照（P0-P3 一次性改造，2026-07-19 16:00）
已完成的 patch：
1. HtmlChapter 新增 props：twoUp('auto'|'single'|'double')、onPageChanged、onPageNav、onReady；onMessage 已路由 pageChanged/pageNav/ready
2. fullHtml pageFlip 分支：去掉静态 columns（改由运行时 __pf 引擎设置），padding bottom 64px，will-change transform；非翻页分支 body>* max-width 42em auto；web 分支 div 42em 居中
3. pageFlipScript v2 完成：transform 翻页（260ms cubic-bezier(0.25,0.1,0.25,1)）、applyLayout（双页>=900或twoUp='double'：column-count 2 gap 2m；单页：动态 pad 限 42em 行长）、__setPage/__goToPage/__getAnchor/__restoreAnchor（字符偏移锚定）、measure(keepAnchor) resize 恢复、点击分区 25/50/25 capture 阶段（跳过 img/a/选区）、keydown ArrowLeft/Right/PageUp/Down/Space、图片延迟加载重排、ready 消息
4. 主屏：bookRef/bookId/sectionCount、chapterIdxRef；autoSave 30s 用 ref 不再依赖 book；bookmarks 初值 book?.bookmarks，toggleBookmark 回调（updateBook 持久化）；twoUp state + 设置持久化（SETTINGS_KEY 加 twoUp 字段）；高亮注入由 engineReady 驱动（150ms）；restorePageRef（初值 lastPage；goPrevChapter 设 -1=末页；goNextChapter 设 null）；chapterCacheRef LRU5 + 相邻±1 预载（600ms idle）；进度条 scrub 预览（scrubChapter state）+ release 提交 + navHistory push；章节加载 effect 依赖 [bookId, chapterIdx]（频闪根治）保留旧帧；位置保存 effect 用 ref；handleInternalLink：pendingAnchorRef + navHistory push + __setPage 锚点翻页
待做：
- [ ] 删除 setCurrentPageInChapter 残留 4 处（L1591 topBar 返回按钮、L1692 阅读区 onPageInfo、L1949/1966 bottomBar 章节按钮）+ 状态定义已删
- [ ] 需要新增 pendingAnchorRef 定义（handleInternalLink 已引用！）
- [ ] onPageInfo 处理：totalPages 回报后若 restorePageRef.current 非 null → __setPage(min(lastPage,total-1)) 或 -1=total-1；处理 engineReady++（onReady 回调）；处理 onPageChanged → setCurrentPage+ref；onPageNav → goPrev/NextPage
- [ ] goNextPage/goPrevPage 已存在用 __goToPage（保留即可，透传动画）
- [ ] chrome overlay 化：styles.topBar 加 absolute top0 zIndex20 + paddingTop insets.top；ScreenContainer edges 改 ["left","right"]；chrome fade（可选 Animated.View opacity）
- [ ] 常驻页脚极简页码 + Apple 风双态：宽屏(>=700)悬浮 ‹ › 箭头（absolute 垂直居中 zIndex15）
- [ ] bottomBar 改造：滑块保留（已做 scrub）、页码文案「第 x / y 页」、AI 提取按钮保留
- [ ] 设置面板：删重复字号块（第一个 Font Size L1818 附近）、加双页 segmented（自动/单页/双页）、翻页模式开关保留
- [ ] TOC 跳转 push navHistory + restorePageRef=null
- [ ] HtmlChapter 调用处传新 props：twoUp、onPageChanged、onPageNav、onReady
- [ ] pageFlipGesture：totalPages>1 时 onEnd 调 goNext/PrevPage（已有）；swipe 动画保留
- [ ] web 分支 reading 区域也要能翻页? （web 用滚动即可，不影响）

## 进度快照 2026-07-19 16:06（P0+P1 主体已完成，TS 0 错误）
已完成（全部 TS 0 错误通过）：
- P0 全部：bookRef/chapterIdxRef、autoSave ref 化、章节加载 effect 依赖 [bookId, chapterIdx]（频闪根治）、进度条 scrub 预览+release 提交+navHistory push、书签 toggleBookmark 持久化（updateBook）、高亮 engineReady 驱动（150ms）、topBar/bottomBar overlay 化（styles.topBarOverlay/bottomBarOverlay absolute zIndex20 + paddingTop insets.top+8）、ScreenContainer reading 相 edges=["left","right"]、阅读区 paddingTop: insets.top
- P1 全部：pageFlipScript v2 transform 引擎（L411-570：__setPage 260ms cubic-bezier、点击分区 25/50/25 capture、keydown、__getAnchor/__restoreAnchor、measure/resize、图片重排）、HtmlChapter props（twoUp/onPageChanged/onPageNav/onReady L250/270-278 + onMessage 路由 L607-609）、handlePageInfo（restorePageRef 恢复：-1=末页）/handlePageChanged/handlePageNav/handleEngineReady（pendingAnchor 跳转）、goPrev/NextChapter ref 化（prev 落末页 restorePageRef=-1）、内链 handleInternalLink navHistory+pendingAnchorRef、bottomBar 箭头改翻页语义（goPrev/NextPage + 双重 disabled 条件）、返回按钮 restorePageRef=prev.page
- P2 部分：常驻页脚极简页码（!chromeVisible 时显示 x/y页·本章最后一页）、宽屏>=700 悬浮 ‹ › 箭头（floatArrow 样式）、双页引擎（TWOUP_MODE/TWOUP_MIN_W 900/isTwoUp/applyLayout 双列 gap 2m）、单页行长 42em 限制（动态 pad）、scrub 气泡文案（章节标题预览）
剩余待做：
- [ ] fullHtml useMemo deps 检查 OK（twoUpMode 不在 fullHtml 中使用，引擎脚本用 TWOUP_MODE=${JSON.stringify(twoUpMode)} —— 但 pageFlipScript 不是 useMemo，每 render 重算 OK；注意：twoUp 变化时需 WebView 重载才生效 → 简单方案：HtmlChapter key 加 twoUp）——决定：阅读中改 twoUp 设置后需触发重载，在 HtmlChapter 调用处 key={`ch-${chapterIdx}-${twoUp}`}（key 变化强制 remount，可接受因为是显式设置操作）
- [ ] 设置面板：删重复字号块、加双页 segmented（自动/单页/双页 setTwoUp）L2150 附近
- [ ] TOC 跳转 push navHistory + restorePageRef=null（tocOpen drawer 内 onPress）
- [ ] chrome fade 动画（可选：直接条件渲染已 OK，Apple 也是快速出现；跳过）
- [ ] 验证：tsc/vitest/截图
- [ ] todo.md 更新 + checkpoint
注意事项：
- setCurrentPageInChapter 已全部清除；currentPageInChapter state 已删
- web 分支 div 42em 居中已做
- styles.pageFooter 已加

## 进度快照 2026-07-19 16:09（P0-P3 全部完成，TS 0 错误）
P2 收尾完成：删除重复字号块、双页对开 segmented（自动/单页/双页，仅 pageFlipMode 显示）、TOC 跳转 push navHistory+位置归零+书签图标显示、HtmlChapter key={`ch-${chapterIdx}-${twoUp}`}（twoUp 变化强制重建引擎）
P3 全部完成：
- 章节 LRU 缓存（5章）+ 相邻章 600ms 空闲预载（L955-1008，Phase1 已写好确认无误）
- 图片 file:// 直读（rewriteImagesToFileUris，iOS/Android 走快路径，替代 base64 内联防内存膨胀），WebView 加 allowFileAccessFromFileURLs + allowingReadAccessToURL=documentDirectory
- 高亮注入 engineReady 驱动（L874-882，替代 700ms 盲等）
- 锚点跳转 pendingAnchorRef + handleEngineReady 内 __setPage 定位
- 状态合并：currentPageRef/totalPagesRef/bookRef/chapterIdxRef 全 ref 化，无冗余渲染
剩余：vitest + 截图验证 → todo.md 更新 → checkpoint → 交付（暂不 EAS Build）
风险提醒（交付时告知用户）：
- file:// 图片直读在 iOS WKWebView 需真机验证（若图片不显示回退方案是恢复 inlineImagesAsBase64——代码保留未删）
- 双页模式 Web 预览无法验证（pageFlipMode 仅 native），需 TestFlight 真机验证

## EAS Build 56 任务（2026-07-19，来自 AGENTS.md 关键信息）
- 云电脑 session 前缀：cloud-pc-8nvfg1b9:{sessionId}，仓库在云电脑 ~/cocktail-r（relaxRG/cocktail-r）
- EXPO_TOKEN（Build37 用过）: x_5a1Tebvrgg5bHFMcdBmkjHj5IutY-4zBRfzpHs
- 提交命令: EXPO_TOKEN=<token> eas submit --platform ios --profile production --latest --non-interactive
- 触发构建: eas build --platform ios --profile production --non-interactive --no-wait
- Apple API Key: ~/.apple-creds/AuthKey_5W7X3LW537.p8 (Key ID 5W7X3LW537, Issuer 4c2aa939-390b-4d72-afef-7724d6238127)
- App ID: 6788653669; TestFlight: https://appstoreconnect.apple.com/apps/6788653669/testflight/ios
- 上次 Build #37 (appBuildVersion 37) git 34c82e3 — 用户说"build 56"是 TestFlight 上的 CFBundleVersion 序号（本地 appBuildVersion 自增）
- 流程：沙盒推代码到 GitHub → 云电脑 git pull → eas build → eas submit → 更新 AGENTS.md

## Build 56 实时状态（2026-07-20 00:xx）
- EAS Build 已完成：Status finished，Build number 55（EAS 显示，TestFlight 递增后为 56），ID dc8ca4d9-951c-4cf6-9305-cd04af1aee75，git f641f18
- 构建从沙盒触发成功（云电脑 sidecar 断连不影响：EAS 构建在 Expo 云端）
- 待完成：eas submit 提交 TestFlight。沙盒缺 /home/ubuntu/.apple-creds/AuthKey_SS7N6Q2U48.p8（eas.json 引用），云电脑挂载也断了
- eas.json submit 配置：ascAppId 6788653669, KeyId SS7N6Q2U48, Issuer 4c2aa939-390b-4d72-afef-7724d6238127, keyPath /home/ubuntu/.apple-creds/AuthKey_SS7N6Q2U48.p8
- 备选：云电脑 ~/.apple-creds/ 有 AuthKey_5W7X3LW537.p8（旧 key）+ 可能有 SS7N6Q2U48；或用 ASC_API_KEY 环境变量方式
- Manus UI 的"提交到 TestFlight"按钮报错 exit status 127 与我们流程无关
- EXPO_TOKEN: x_5a1Tebvrgg5bHFMcdBmkjHj5IutY-4zBRfzpHs（有效，rgsds-team）
- 提交结果：submission 178ed821-01f4-469e-a568-5705e2560d58 → FINISHED（无错误），已上传 App Store Connect，等 Apple 处理后现身 TestFlight
- p8 私钥由用户上传，已放 /home/ubuntu/.apple-creds/AuthKey_SS7N6Q2U48.p8

## 新 Bug 报告（2026-07-20 用户 TestFlight Build 56 反馈）
1. 配方详情页「装饰」行（如"脱水菠萝"）点击后跳转错误：直接打开「编辑酒款」表单（bottle-form/编辑模态），而非详情页
2. 期望：装饰智能链接 → 对应条目详情页。用户截图显示"脱水菠萝"链接的条目其实是自制库(homemade)装饰分区条目（编辑表单显示所属库=自制库、分区=装饰），但打开的表单标题是「编辑酒款」
3. 疑似根因：装饰行的 link 解析将 homemade 装饰条目当成 bottle，或跳转路由直接指向编辑表单

## Bug 定位发现（装饰跳转）
- recipe/[id].tsx 572-660：装饰行渲染，link=smartLinkIngredient(part.name,bottles,preps)
  - link.kind==="prep" → /homemade/[id]；kind==="bottle" → /bottle/[id]（路由本身指向详情页，没有直接推编辑表单）
- smart-link.ts: 匹配顺序 exact bottle > exact prep > ... 
- 用户截图：条目"脱水菠萝"在编辑表单里显示 所属库=自制库(homemade?) 分区=装饰。但表单标题是「编辑酒款」
- 关键疑点：用户的"脱水菠萝"其实存在 bottles 数组里（library 字段="homemade"），不是 preps！app 数据模型可能是 Bottle.library: base/bottle/ingredient/homemade。所以 smartLink 返回 kind=bottle → 跳 /bottle/[id]
- 需检查 /bottle/[id].tsx 对 library==="homemade" 的处理：可能重定向到编辑表单或 bottle-form

## Bug 1+2 根因确认（2026-07-20）
1. 数据模型：Bottle.libraryOverride='homemade' 的酒款条目在自制库 tab 以"虚拟 prep"（id=bottle-override-{bottleId}）展示（homemade.tsx 85-112）
2. 自制库列表点击虚拟条目：homemade.tsx 1321-1328 直接 router.push("/bottle-form", {id}) → 打开「编辑酒款」表单 ← 这就是用户看到「编辑酒款」表单、所属库=自制库的现象（Bug 2 直接根因）
3. recipe 详情装饰行（recipe/[id].tsx 640-643）：smartLink auto 模式排除了 libraryOverride='homemade' 的酒款（smart-link.ts 79），也匹配不到虚拟 prep（虚拟条目只在 homemade.tsx 屏内生成，不进 store）→ "脱水菠萝"这类条目要么无链接、要么 fuzzy 匹配到别的条目
4. 结论：override 类条目「没有详情页」——bottle/[id].tsx 是正常详情页，homemade/[id].tsx getPrep 查不到虚拟 id
5. 修复方向：
   A. homemade.tsx 虚拟条目点击 → 跳 /bottle/[id]（详情页）而非 bottle-form（编辑表单）✅ 最小改动
   B. smart-link.ts auto 模式不再排除 override 酒款（kind=bottle 正常返回）→ 装饰行可点、跳 /bottle/[id] 详情页 ✅
   C. bottle/[id].tsx 对 libraryOverride='homemade' 条目显示自制库风格徽章（可选增强）

## Bug 3 待确认：book-reader.tsx file:// 图片

## Bug 3 根因确认
- book-reader.tsx 950-956：iOS/Android 走 rewriteImagesToFileUris（file:// 直读），仅 web 走 base64
- WebView source={html, baseUrl}：iOS 上 loadHTMLString + baseURL 模式下，WKWebView 对 file:// 子资源的访问受 App Sandbox+WKWebView 安全策略限制，allowingReadAccessToURL 仅对 loadFileURL 生效 → 真机图片全部载入失败显示问号
- 修复：953 行条件改为仅 Android 走 file:// 直读（Android WebView allowFileAccess 可靠），iOS 与 web 一样走 inlineImagesAsBase64（该函数已有 LRU 缓存章节 HTML，性能可接受）

## Bug 4 根因（AI 补全未填风格 style）
- Worker enrich-bottle：BOTTLE_STYLES_MAP 白名单校验 styleList.includes(rawStyle)，不在列表返回 ""（worker.js 622-629）
- Worker 朗姆值域：["White/Blanco","Gold/Oro","Dark/Añejo",...]
- 客户端 taxonomy.tsx 朗姆值域：name=["White / Light","Gold","Aged / Añejo","Dark / Black",...]（chip 用 name 匹配 active = style === s）
- 三方值域不一致：即使 AI 返回了合法值（如 "White/Blanco"），客户端 chip "White / Light" 不匹配 → 显示为未选中（只能落到自定义输入框，但 applyField 只 setStyle，chip 不亮）
- 另外 Worker 校验失败时直接抹为 "" → aiResult.style 为空 → buildAiFields 里 if(aiResult.style) 直接不出现该字段
- 修复：统一值域为客户端 taxonomy 的 name 列表（Worker map 从 taxonomy 生成/同步），Worker 校验失败时不抹空而是模糊映射（忽略大小写/斜杠空格差异），客户端 applyField 时对 style 做 taxonomy 兜底映射

## Bug 5 根因（Mac→手机不同步）候选链
1. Pull 只发生在 App 冷启动的 performSync（provider.tsx 172-182 仅 mount 一次）；iOS 上 App 常驻后台数天不重启 → 手机永远拉不到 Mac 的更新。无 AppState 前台激活 pull、无周期 pull。
2. 整键 last-write-wins：cocktail.bottles 整个 JSON 是一个 key。若手机先冷启动 pull 后又本地改动 push，会用旧合并值覆盖 Mac 的新值（时间戳更新者胜，字段级丢失）。
3. Mac 端身份问题：Mac（Expo Web 或 Mac Catalyst?）getOrCreateDevice 若未走配对码而是自动 register，会创建独立 groupId → 两台设备根本不在同一组。web 用 AsyncStorage 存身份，清浏览器缓存后重新注册成新组。
- 验证途径：查 D1 devices 表设备/组分布 + sync_data 更新时间

## Bug 5 深入发现（Worker 端，源码位置：云电脑 ~/cf-worker-deploy/worker.js）
- handleSyncPull（约1246行）：支持 body.since 增量拉取（客户端未用），返回 entries+tombstones+serverTime
- handleSyncPush（约1288行）：INSERT OR REPLACE 直接覆盖，无 client_updated_at 比较！服务器不做 LWW 校验（客户端旧数据可覆盖新数据）
- handleDeviceRegister（约1310行）：每次 register 无条件创建新 groupId（Mac 端如果没走配对码 → 独立组，永不互通）
- verifyDevice：查库比对 token（服务端生成的随机 token，不是客户端 HMAC）
- D1 直查失败：两个 CF token 都无 D1 REST 权限（7403/authentication error），wrangler d1 execute 也 7403。无法直接验证用户的设备分组情况 → 方案文档中列为"待用户在 App 内检查设备管理页"
- 客户端拉取只在 App 冷启动 mount 时执行一次（provider.tsx 172），无 AppState 前台监听、无周期 pull → iOS App 挂后台多天不会重新 pull ← Mac 改动到不了手机的主要根因（若两台设备同组）
- 客户端 push：脏键 debounce 3s；runInitialSync 为整键 LWW（clientUpdatedAt 比较）
- 修复方向（Bug 5）：
  1. AppState 监听：active 时若距上次 pull > 60s 则增量 pull（用 since=lastPulledAt）+ 合并 + store reload
  2. 服务端 push 加 LWW 守卫：INSERT ... ON CONFLICT DO UPDATE SET ... WHERE excluded.client_updated_at > sync_data.client_updated_at
  3. 设备管理页显著显示 groupId 短码，方便用户核对两台设备是否同组；若不同组引导重新配对
  4. 已有"立即同步"按钮（Build 55）调 retrySync → performSync 全量 pull，可作为手动兜底

## Bug 4 客户端 applyField 位置：bottle-form.tsx 194 行 setStyle(aiResult.style)；chip active 判断在 898 行 style === s（s=d.name）
## Worker BOTTLE_STYLES_MAP 位置：worker.js 189 行；enrich-bottle style 白名单校验 621-629 行
