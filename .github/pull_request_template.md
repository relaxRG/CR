## 变更摘要

请说明本 PR 的业务目的、涉及的模块和用户可见影响。

## 防崩溃与数据安全检查

- [ ] 我确认新增/修改的页面已在对应 Feature / Tab Provider 边界中装配所需 Context。
- [ ] 我确认所有新增 `AsyncStorage` 读取、JSON 解析、同步 reload 和 `Promise.all` 都有拒绝处理及运行时数据形状校验。
- [ ] 我确认原生模块、权限、文件导入、外部链接和导航失败均有可恢复降级，不会直接抛出。
- [ ] 我确认修改启动、水合、深链或 Context 后，已新增或更新真实运行时回归测试。
- [ ] 我确认未移除根级错误边界或绕过 Provider Stability Matrix。

## 存储、性能与测试

- [ ] 如变更存储/同步/备份/恢复，我已运行 `pnpm ci:storage` 并提交受控注册表与架构产物。
- [ ] 如变更长列表、图表、工作区、月份切换或导入，我已运行对应 Profiler / 压力测试。
- [ ] `pnpm lint` 通过。
- [ ] `pnpm check` 通过。
- [ ] `pnpm test` 通过。
- [ ] `git diff --check` 通过。

## 真机与发布影响

- [ ] 本 PR 不影响原生行为或发布路径。
- [ ] 若影响原生行为，我已记录应在 TestFlight 验证的页面、权限/网络条件和预期结果。

## 关联证据

请粘贴相关测试命令输出、Profiler 预算、崩溃报告编号或截图链接。
