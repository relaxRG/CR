# Cocktail R 全 App UI/UX 优化方案（v3 · 供确认）

> 基于当前 checkpoint 6d0e4da4 对全部 24 个页面的逐一代码盘点，结合您提供的截图（我的页、酒库页、自制库页、Crypto 记账 App 参考图，以及新增的标签管理参考图 IMG_3205/3206/3207 与 iOS 颜色选择器参考图 IMG_3208/3209）。方案分为四部分：**设计规范（统一语言）→ 逐页改造清单 → 两项专项重构 → 分优先级实施计划**。确认后实施，Build 57 仍等您单独指令。

---

## 一、现状诊断：为什么会"违和、不同步"

对全部页面的盘点显示，问题不是个别页面的样式错误，而是**缺少一套全局设计规范**，各页面在多次迭代中各自演化：

| 维度 | 现状 | 问题 |
|------|------|------|
| 圆角 | 各页混用 4/6/8/9/10/12/16/24/999 共 9 种圆角值 | 视觉碎片化，卡片"松紧"不一 |
| 卡片 | 有的白底+1px 边框（酒库/自制库/购物清单），有的无边框纯投影（我的页），有的灰底 | 同一 App 里出现三种卡片质感 |
| 大标题 | 酒单/我的 34px，酒库壳 30px，书库 30px 且头部结构自管 | 四个 Tab 首屏标题大小不一 |
| 搜索框 | 酒库=灰底无边框 42 高；自制库=白底带边框 44 高；配方/书库又是另一种 | 同类控件三种形态 |
| AI 补全入口 | 酒库=霸屏双大按钮；自制库=单行 Banner；位置一上一下 | 功能相同、形态迥异 |
| 危险操作 | 我的页 2 行红色实心图标 + 设备管理页、数据管理页共 12 处红色用法不一 | 红色滥用、刺眼 |
| 底部 Tab | 系统默认样式（白底+顶部细线） | 与参考图的"浮岛"质感差距最大的一处 |
| FAB | 酒单/配方/酒库各自定义 styles.fab，阴影与尺寸略有差别 | 复制粘贴式重复代码 |
| 空状态 | 仅自制库/购物清单有插图空状态，其余页面为纯文字或空白 | 体验参差 |

---

## 二、设计规范（Design Tokens · 一次定义，全局生效）

参照您认可的 Crypto 记账 App 与 iOS HIG，建立以下规范并落为共享常量/组件，后续所有页面从规范取值：

### 2.1 色彩

| 用途 | 值 | 说明 |
|------|-----|------|
| 页面背景 | `#F2F2F7`（现状保留） | iOS 系统分组背景灰 |
| 卡片 | 纯白、**无边框**、阴影 `opacity 0.05 / radius 8 / offset(0,2)` | 替代现有 1px 边框 |
| 主色 | 现有 iOS 蓝 `#0A7EA4→#007AFF` 统一为 `#007AFF` | 参考图为黄色，我们保留蓝色酒饮气质 |
| 危险红 | 仅用于**文字与确认弹窗**，禁止大面积实心红底图标 | iOS 设置App 惯例 |
| 功能色图标底 | 蓝/紫/绿/橙小圆角方块（我的页现状保留，仅去掉红色两枚） | 参考截图 1 |

### 2.2 形状与间距

| Token | 值 | 应用 |
|-------|-----|------|
| `radius.card` | **20** | 所有列表卡片、分组卡片 |
| `radius.control` | 12 | 搜索框、小按钮、输入框 |
| `radius.pill` | 999 | chips、胶囊按钮、FAB |
| 页边距 | 20（px-5，现状保留） | 全页统一 |
| 卡片内边距 | 16 | 统一 |

### 2.3 部件规范（落为共享组件）

1. **`AppCard`**：白底、圆角 20、无边框、轻阴影——替换全部"白底+边框"卡片。
2. **`SearchBar`**：灰底（`border+44` 色）、无边框、圆角 12、高 42、放大镜+清除按钮——酒库现有样式提为组件，配方/自制库/书库/购物清单全部换用。
3. **`LargeTitle`**：34px/700/行高 41 + 副标题 13px muted——四个 Tab 首屏统一（书库头部结构同步接入）。
4. **`FloatingTabBar`（浮岛式底部 Tab）**：左右留边 16、圆角 28、白底、阴影、悬浮于内容上方，选中 Tab 图标+文字高亮蓝色并带浅蓝底块（参考截图 4）。各列表页底部 padding 同步加大避免遮挡。
5. **`Fab`**：统一组件，56 圆形、主色、阴影 `opacity 0.3/radius 12`，位置右下距浮岛 Tab 上方 16——三处重复实现合并。
6. **`EmptyState`**：图标/emoji + 标题 + 说明 + 可选行动按钮——全部列表页统一接入。
7. **`DangerRow`**：iOS 标准"居中红字按钮"（无图标实心底），统一危险操作形态。

---

## 三、逐页改造清单

### P0 · 本次核心（您直接反馈的三项）

| 页面 | 改造点 |
|------|--------|
| 我的 me.tsx | ① 「退出同步组」「退出并清除数据」移出主卡片 → 收进「设备管理」二级页底部危险区，改 `DangerRow` 居中红字样式，二次确认保留；② 主卡片只剩 iPhone 同步状态 + 设备管理，图标底色保持蓝/紫 |
| 酒库 bottles.tsx | ① 移除霸屏的「逐条审核+自动填空白」双按钮 Banner → 筛选行右侧紧凑「✨ AI 补全 (N)」胶囊，点击弹**底部抽屉**（逐条审核 / 自动填空白，附模式说明）；② 搜索框提为 `SearchBar` 组件 |
| 自制库 homemade.tsx | ① 移除「N 条待补全」Banner → 同款「✨ AI 补全 (N)」胶囊 + 同款抽屉（自制库的补全并入"自动填空白"模式）；② 搜索框换 `SearchBar`，位置/高度与酒库完全一致；③ 二级切换器、多选按钮排布对齐酒库 |

### P1 · 全局统一（设计规范落地）

| 范围 | 改造点 |
|------|--------|
| 底部 Tab | 换 `FloatingTabBar` 浮岛胶囊样式（改动最大的一处，涉及所有 Tab 页底部 padding 适配） |
| 四个 Tab 首屏 | `LargeTitle` 统一 34px；书库头部接入统一结构（保留其排序/视图切换按钮） |
| 全部列表卡片 | 酒单、配方、酒库、自制库、书库、购物清单卡片换 `AppCard`（去边框、圆角 20、轻阴影） |
| 搜索框 | 配方、书库、购物清单换 `SearchBar` |
| FAB | 酒单/配方/酒库合并为统一 `Fab` 组件（自制库/书库如无 FAB 维持现状） |
| 危险红收敛 | device-manager、data-manager 页共 12 处红色用法按 `DangerRow` 规范收敛 |
| 标签管理 | 重构为「彩色 chip 墙 + 可折叠分组」形态（详见第四部分专项 A） |
| 颜色选择器 | 升级为 iOS 系统风格三模式选择器（详见第四部分专项 B） |

### P2 · 体验补全（可选，默认一并做）

| 范围 | 改造点 |
|------|--------|
| 空状态 | 酒单/配方/酒库/书库接入 `EmptyState`（自制库/购物清单已有，样式对齐） |
| 详情页 | recipe/bottle/homemade 详情页内分组卡片同步 `AppCard` 质感（结构不动） |
| 表单页 | recipe/bottle/homemade-form 输入区圆角与间距对齐 `radius.control`（不改布局与逻辑） |
| 按压反馈 | 列表卡片统一 `opacity 0.7`、主按钮统一 `scale 0.97`+轻震动（现状大体已有，查漏补缺） |

### 明确不改的

书页阅读器（刚完成 v2 升级）、所有业务逻辑与数据结构、酒单三视图切换、配方成本计算展示逻辑、导入流程。

---

## 四、两项专项重构（您新增的两组参考截图）

### 专项 A · 标签管理页重构（参考 IMG_3205/3206/3207）

现状 `tags.tsx`（967 行）为顶部横滚 segment 切换六个维度 + 逐行列表编辑：每行一个标签，改名、换色、移动、排序都藏在行内展开区里，信息密度低、层级感弱。按参考截图重构为「彩色 chip 墙 + 可折叠分组」形态：

| 区域 | 新形态 |
|------|--------|
| 页面骨架 | 浅灰底 + 左上圆形返回按钮 + 居中标题 + 右上「＋ 添加」胶囊按钮 |
| 维度切换 | 六个维度保留，改为与酒库筛选行同款的胶囊 chips，替代现有横滚 segment |
| 分组卡片 | 每个分组一张 `AppCard`：头部 = 组名 + 数量徽章 + 折叠箭头 + 「⋯」菜单（重命名/删除/上移/下移）；点击头部整行折叠/展开，折叠状态持久化 |
| 标签 chip | 组内为**彩色 chip 墙**（自动换行）：每个 chip 用自身颜色的淡底（约 12% 透明度）+ 同色文字的圆角胶囊；点击 chip 弹出编辑抽屉（改名/改色/换组/排序/删除） |
| 组尾添加 | 每组末尾一枚「＋ 添加」虚线边框 chip，点击直接在该组内新建标签 |
| 未分组区 | 未归组的标签收在末尾「未分组」卡片，形态一致 |
| 右上添加 | ActionSheet 两选项：**添加标签**（底部抽屉：名称 + 选组 + 颜色选择器）/ **添加分组**（名称输入） |

标签与分组的数据模型（六维度、groupId、排序字段）**完全不动**，只改呈现层与交互动线。`card-tag-settings.tsx`（卡片标签显示页）同步对齐新视觉（AppCard + 同款 chip 样式），结构不动。

### 专项 B · 颜色选择器升级为 iOS 系统风格（参考 IMG_3208/3209）

现状 `components/color-picker.tsx` 的 `ColorPickerPanel`（376 行）为内嵌式「预设色块 + HSV 三滑块 + Hex 输入」，展开后偏长、质感一般。重写为 iOS 系统颜色选择器同款的**底部抽屉三模式面板**：

| 区域 | 新形态 |
|------|--------|
| 容器 | 底部抽屉大圆角面板，顶部栏：标题「颜色」居中 + 右上「×」关闭 |
| 模式切换 | iOS Segmented 三档：**网格 | 光谱 | 滑块** |
| 网格 | 12 列 × 10 行色块矩阵：顶行灰阶（白→黑），下方每列一个色相、自上而下由深到浅；色块无间隙贴合、矩阵整体圆角，当前色块白框高亮 |
| 光谱 | 连续色域渐变面板（色相 × 明度）可拖动取色，白圈指示当前取色点 |
| 滑块 | 红/绿/蓝三条**渐变轨道**滑块（轨道实时渲染该通道 0→255 的颜色变化）+ 右侧数值框（0–255 可输入）；下方「sRGB 十六进制颜色 #」+ Hex 输入框 |
| 底部区 | 左侧当前颜色大预览方块 + 右侧**常用色圆点行**（默认黑/蓝/绿/黄/红）+「＋」把当前色存为常用色（AsyncStorage 持久化，长按可删除自定义色） |

组件对外接口保持 `value/onChange` 不变，`tags.tsx`、`card-tag-settings.tsx` 及专项 A 的新建/编辑标签抽屉全部换用，App 内所有取色场景统一为这一个组件。

---

## 五、实施计划与验证

| 步骤 | 内容 | 风险 |
|------|------|------|
| 1 | 建 tokens + 9 个共享组件（AppCard/SearchBar/LargeTitle/FloatingTabBar/Fab/EmptyState/DangerRow + TagChipWall + IOSColorPicker） | 低 |
| 2 | P0 三项（您的直接反馈） | 低 |
| 3 | P1 浮岛 Tab + 大标题 + 卡片/搜索框/FAB 统一 | 中（Tab 栏影响全部页面底部间距，逐页截图验证） |
| 4 | 专项 A 标签管理重构 + 专项 B 颜色选择器（含 card-tag-settings 对齐） | 中（tags.tsx 改动面大，但数据层零改动，可回滚） |
| 5 | P2 体验补全 | 低 |
| 6 | TypeScript 检查 + 35 项测试 + 各 Tab 页多尺寸截图走查 | — |

全部通过后保存 checkpoint。**EAS Build 57 触发仍等您单独指令。**

### 请确认

1. **整体方案是否按 P0+P1+P2 全做**？（也可先 P0+P1，P2 下版）
2. 浮岛式底部 Tab 栏确认要做吗？（视觉提升最大，改动也最大）
3. 主色统一为 iOS 标准蓝 `#007AFF` 是否可以？（现状是偏青的 `#0a7ea4`，两者接近，统一后与系统控件更协调）
4. 专项 A（标签管理 chip 墙重构）与专项 B（iOS 颜色选择器）已按您的参考截图并入方案，随 P1 一起实施，是否确认？
