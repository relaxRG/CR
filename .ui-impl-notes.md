# UI 实施关键盘点（2026-07-20）

## 已确认的现状
- theme.config.js：已是 #007AFF / #F2F2F7 / #FFFFFF，主色已对
- _layout.tsx：默认 Tab 栏，需改为 FloatingTabBar
- me.tsx（387行）：退出同步组（colors.error 红底图标）+ 退出并清除（#FF3B30 红底图标）在主卡片内，需移入 device-manager.tsx 底部
- device-manager.tsx（795行）：ScrollView 在 ~680 行结束，危险区插在 </ScrollView> 前
- bottles.tsx：Banner 在 ~866 行（两个 Pressable 并排），需改为筛选行右侧胶囊
- homemade.tsx：Banner 在 ~678 行（单行横幅），需改为同款胶囊
- recipes.tsx：搜索框 ~578 行（bg-surface border border-border rounded-xl），需换 SearchBar 组件
- books.tsx（text-3xl 30px 标题），需改 34px；搜索框 bg-surface border border-border rounded-lg
- bottles.tsx：无大标题（直接从 segment 切换器开始），需加 LargeTitle
- homemade.tsx：无大标题，需加 LargeTitle
- FAB：recipes.tsx ~840行，bottles.tsx ~1248行，homemade.tsx ~952行，三处完全相同样式
- data-manager.tsx：~219行 colors.error 红底图标 + 红字，需改 DangerRow

## 搜索框现状对比
- bottles.tsx：灰底（colors.border+"44"）无边框 圆角12 高42 ✓（这是标准）
- homemade.tsx：白底（bg-surface）有边框（border-border）圆角xl 高44 ✗
- recipes.tsx：白底（bg-surface）有边框（border-border）圆角xl 无固定高 ✗
- books.tsx：白底（bg-surface）有边框（border-border）rounded-lg ✗

## 共享组件设计
### SearchBar
- 灰底 colors.border+"44"，无边框，圆角12，高42
- 放大镜图标 + TextInput + 清除按钮

### AppCard
- 白底 surface，圆角20，无边框，轻阴影
- 替换所有 bg-surface border border-border rounded-xl/2xl

### FloatingTabBar
- 左右边距16，圆角28，白底，阴影，悬浮
- 选中：浅蓝底块 + 蓝色图标+文字
- 各页 bottom padding 需增加到 ~90

### Fab
- 56圆形，primary色，阴影，right:20，bottom 根据 FloatingTabBar 调整

### LargeTitle
- fontSize:34，fontWeight:700，lineHeight:41
- 副标题 fontSize:13，color:muted

### DangerRow
- 居中红字按钮，无图标实心底
- onPress 触发确认弹窗

### EmptyState
- 图标/emoji + 标题 + 说明 + 可选按钮

### IOSColorPicker（专项B）
- 底部抽屉，顶部标题+关闭
- Segmented 三模式：网格/光谱/滑块
- 底部常用色 + 预览方块

### TagChipWall（专项A）
- flex-wrap chip 墙
- 每个 chip：自身颜色淡底12% + 同色文字 + 圆角胶囊

## tags.tsx 重构要点
- 967行，横滚 segment + 列表行式
- 重构为：胶囊维度切换 + 分组卡片（可折叠）+ chip 墙
- 数据层完全不动（TagGroup、TagKind、groupId 等）
- 3处 ColorPickerPanel 调用需换为 IOSColorPicker

## card-tag-settings.tsx 重构要点
- 502行，1处 ColorPickerPanel 调用
- 对齐新视觉：AppCard + chip 样式

## me.tsx 危险操作迁移
- handleLeaveGroup 函数（~38行）和 handleLeaveGroupAndClear 函数（~55行）需复制到 device-manager.tsx
- me.tsx 中删除这两个 Pressable 行（~135-175行）
- device-manager.tsx 在 ScrollView 末尾（~680行）前插入危险区

## bottles.tsx Banner 改造
- 删除 ~866-900行 的两个并排 Pressable Banner
- 在筛选行（QuickFilterChips leading 区域）的 Filter 按钮后面加「✨ AI 补全 (N)」胶囊
- 点击弹底部抽屉（Modal）展示两个选项

## homemade.tsx Banner 改造
- 删除 ~678-706行 的 Banner 区域
- 在筛选行 QuickFilterChips leading 区域加「✨ AI 补全 (N)」胶囊
