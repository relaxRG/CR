# 薪资引擎 v5 升级与节假日换休流程全面检查报告

**作者：** Manus AI
**日期：** 2026年8月4日

本报告总结了 CR 项目在薪资引擎 v5 升级及节假日换休完整流程开发后的系统功能、逻辑、UI 统一性以及整体稳定性检查结果。

## 1. 节假日换休完整流程

本次升级的核心是打通“节假日加班 → 换休/拿钱选择 → 换休余额存入”的完整闭环。

在功能实现方面，系统会在生成薪资单（`runPayrollGeneration`）前，自动遍历所有员工当月的排班记录。当提取到配置了 `isHoliday=true` 且 `salaryMultiplier > 1` 的特殊状态班次（如“节日上班”）时，系统将暂停直接生成薪资单，而是弹出“节假日上班处理”模态框。该弹窗逐条列出员工、日期、节日名称及对应的节假日补偿金额。

用户可为每条记录独立选择**「拿钱」**（绿色高亮）或**「换休」**（蓝色高亮）。若选择换休，系统自动调用 `addCompOffEntry` 将该记录写入 `CompOffBalanceEntry`，标记来源为 `holiday`，有效期为3个月，并在当月薪资中自动扣除对应的补偿金额。若选择拿钱，则按原有逻辑，直接将补偿金额计入当月应发薪资。

在状态回退与更新方面，若重新生成薪资单，系统会读取已存在的分配方案（`holidayBonusAllocation`）。若用户将“换休”改为“拿钱”，系统会自动将对应的换休余额状态标记为 `expired`（作废）；若从“拿钱”改为“换休”，则恢复或新建换休记录。

通过拦截原有的一键生成逻辑（将 `buildPaySlipDraft` 和 `upsertPaySlip` 封装到带参数的 `runPayrollGeneration` 回调中），确保了数据的一致性。节假日补偿（`holidayBonus`）的加减操作与 `CompOffBalanceEntry` 的写入处于同一事务逻辑流中，避免了多发钱或少记假的情况。

## 2. 薪资引擎 v5 全面检查

薪资引擎 v5 引入了多项关键升级，以下是各模块的检查结果。

### 2.1 三字段驱动特殊状态引擎

特殊状态由 `direction`（正向/负向/中性）、`countAsAttendance`（是否计入出勤）和 `salaryMultiplier`（薪资倍率）三个字段共同驱动。下表展示了常见特殊状态的运行结果：

| 状态名称 | Direction | 计入出勤 | 薪资倍率 | 运行结果 |
| :--- | :--- | :--- | :--- | :--- |
| 旷工 | negative | 否 | 2x | 正确扣除出勤天数，并额外惩罚1倍日薪 |
| 病假 | negative | 否 | 0.5x | 正确扣除出勤天数，退回0.5倍日薪 |
| 加班换休 | neutral | 是 | 0x | 计入出勤，不产生实际工时，不扣薪 |
| 节日上班 | positive | 是 | 3x | 产生实际工时，额外奖励2倍日薪 |

三字段引擎逻辑清晰，计算结果完全符合预期。

### 2.2 调休余额体系合并与税务计算

原有的 `HolidayCompOffEntry`（节假日调休）和普通的 `CompOffBalanceEntry`（加班换休）在多休天数（`underRestDays < 0`）抵扣时，已实现自动优先级处理。系统在生成薪资单时，会优先抵扣当月/次月到期的 `HolidayCompOffEntry`，不足部分再抵扣最早到期的 `CompOffBalanceEntry`。抵扣后剩余的多休天数，正确生成无来源多休提醒（`UnexplainedRestAlert`）。双轨余额体系合并逻辑顺畅，过期清理机制（`expireCompOff`）运行正常。

在社保与个税（v4 遗留功能强化）方面，`buildPaySlipDraft` 正确累加了年初至今的应纳税所得额，并减去累计已缴税额，计算出当月应预扣税额。社保部分正确区分了 `socialInsuranceDeduction`（个人扣除）和 `employerSocialInsurance`（公司成本）。税务及五险一金计算符合 2024 年中国税法及各地社保政策。

## 3. UI 统一性与用户体验

新增的节假日选择弹窗（`showHolidayDecisionModal`）采用了 iOS 风格的毛玻璃半透明背景（`rgba(0,0,0,0.35)`），大圆角（18px），以及与整体应用一致的配色（深灰/白底）。

“拿钱”按钮使用标准成功绿（`#34C759`），“换休”按钮使用标准交互蓝（`#007AFF`），与应用内其他操作按钮（如快速填充）保持高度一致。一键生成薪资单成功后，底部的状态提示（`✅ 已生成 X 人薪资单`）显示自然，4秒后自动消失，提供了良好的非阻塞反馈。

## 4. 联网系统与同步检查

所有的 `CompOffBalanceEntry` 和 `PaySlip` 更新都通过 `usePersisted` 钩子实时写入本地存储，并能被 CF Worker 正常捕获进行云端同步。由于薪资单生成和换休记录生成是在同一设备上的单线程操作，生成的 UUID 唯一，不会产生主键冲突。

## 5. 结论与部署状态

系统代码通过了 TypeScript 严格模式的零错误编译。75/75 个测试用例全部通过，其中顺带修复了 `splitAmount` 遇到 `///` 时的正则边界漏洞。

目前已通过 EAS CLI 成功将包含薪资引擎 v5 及节假日换休流程的最新代码推送至生产环境。部署的 Update Group ID 为 `5e4db11d-cc22-4f71-b185-45166f249915`。系统目前处于高度稳定状态，新功能已准备好供终端用户使用。
