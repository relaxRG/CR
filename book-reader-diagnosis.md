# 书库阅读器（book-reader）深度诊断报告

**范围**：`app/book-reader.tsx`（2468 行）、`lib/books/store.tsx`，针对你在 Mac（TestFlight「适用于 iPad 的 App」运行模式）上遇到的翻页失效与自动频闪问题，以及对标 Apple「图书」App 的阅读体验差距。以下所有结论均基于逐行代码审查，尚未做任何修改，等待你的指令。

---

## 一、自动频闪：根因已锁定（严重，必须修）

这是本次审查中最确凿的一个 bug，根因链完整可复现：

1. 阅读器每 30 秒自动保存阅读位置：`updatePosition(book.id, ...)`。
2. `updatePosition` 内部执行 `setBooks(prev.map(...))`，**每次都会生成当前书籍对象的新引用**。
3. 阅读器通过 `books.find(...)` 取得 `book`，引用随之变化。
4. 章节加载 effect 的依赖数组是 `[book, chapterIdx, loadChapter]`——`book` 引用一变，effect 就重跑：`setLoadingChapter(true)` → `setChapterHtml(null)` → **WebView 整个卸载重建** → 白屏一闪，然后重新渲染章节。

也就是说：**每隔 30 秒，页面必然白屏闪烁一次**，这正是你描述的「自动频闪」。此外还有两条叠加触发链使换章时出现"双重/三重加载"：换章时的 `updatePosition` 调用、首次打开书时的 `updateBook(readingStatus)` 调用，都会再次改变 `book` 引用，导致同一章节被连续加载 2–3 次。

**修改方案**：章节加载 effect 的依赖改为 `[book?.id, chapterIdx]`（按 ID 而非对象引用比较），章节数据通过 ref 读取。这是一处 3–5 行的修改，可彻底消除频闪，且不影响任何现有功能。

**频闪的第二来源**：底部进度条拖动使用 `PanResponder`，`onPanResponderMove` 期间每移动一点就 `setChapterIdx` 一次，每次都触发完整的「loading → 读文件 → WebView 重建」流程。快速拖动时会连续闪烁十几次。方案：拖动过程中只更新预览指示（章节号气泡），**松手时才真正跳转**，与 Apple 图书的进度滑块行为一致。

---

## 二、Mac 上无法翻页：根因已锁定（严重，必须修）

当前翻页模式（pageFlipMode）下，**页级翻页的唯一入口是触摸 Pan 滑动手势**。问题在于：

| 现状 | 在 Mac 上的后果 |
|------|----------------|
| 只有 `Gesture.Pan()` 触摸滑动可翻页 | Mac 用鼠标操作，拖动会先被 WebView 内部的文本选择拦截，Pan 手势几乎无法触发 |
| 底部左右箭头按钮**只切章**，不翻页 | 点按钮直接跳到下一章，章内其余页面全部跳过 |
| 无点击边缘翻页热区 | Apple 图书点击屏幕左/右 25% 区域即可翻页，我们没有 |
| 无键盘方向键支持 | Mac/iPad+键盘用户无法用 ←/→ 翻页 |

**修改方案**（三项配合，全面覆盖鼠标、触摸、键盘）：

1. **点击翻页热区**：屏幕左右各 25% 宽度区域，单击即上/下一页（中间 50% 保持唤出工具栏），与 Apple 图书一致。这一项同时惠及 iPhone/iPad/Mac。
2. **底部箭头按钮改为翻页语义**：在翻页模式且本章多页时，按钮执行 `goPrevPage/goNextPage`（到达章首/章尾时自然切章）；长按可直接切章。
3. **键盘支持**：在 WebView 内监听 `keydown`（←/→/空格/PageUp/PageDown），转发给 RN 执行翻页，覆盖 Mac 和外接键盘场景。

---

## 三、单页/双页自动切换：当前完全缺失（新功能）

目前分页布局固定为单栏（`columns: 1`），无论屏幕多宽。这带来两个问题：其一，Mac 宽屏下正文一行长达 200+ 字符（你的截图 1 正是如此），远超舒适行长（约 38–42em），可读性差；其二，与 Apple 图书「横屏/宽屏自动双页对开」的预期不符。

**修改方案**：分页 CSS 改为响应式——视口宽度 ≥ 900px 时自动切为 `columns: 2`（双页对开，中缝留白），< 900px 保持单页；`window.resize` 时重算总页数并保持当前阅读位置。页数计算逻辑（`scrollWidth / innerWidth`）无需改动，天然兼容。同时给正文加 `max-width` 行长上限，即使单页宽屏也不会出现超长行。另可在阅读设置里加「自动/单页/双页」三档手动开关。

---

## 四、界面显示问题清单

| # | 问题 | 现状 | 方案 |
|---|------|------|------|
| 1 | 宽屏行长过长 | 正文无 max-width | 加 42em 行长上限并居中 |
| 2 | 页脚重叠 | 章节标题、页码、进度条挤在底部工具栏，与正文底部 80px padding 各自为政 | 参照 Apple 图书：正文页脚固定显示「第 x / y 页 · 本章最后一页」轻量页码（非工具栏），工具栏仅在点击唤出时覆盖 |
| 3 | 顶栏信息密度低 | 左侧数字含义不明（你截图 1 中的「12」） | 改为「‹ 返回」+ 章节标题居中 + 全书进度百分比 |
| 4 | 进度条粒度粗 | 进度条按章节数等分，拖动只能到章首 | 进度按「章节 + 章内页」加权计算（已部分实现），拖动松手后落到目标章 |
| 5 | 书签不持久 | `bookmarks` 只存在内存 state，退出即丢 | 持久化到书籍元数据（store 已有 `bookmarks` 字段，未接线） |
| 6 | 内链锚点跳转不完整 | 跨章内链跳到章首，未滚动到锚点（代码中有 TODO） | 章节加载完成后注入锚点定位脚本 |

---

## 五、流畅度与性能

1. **WebView `cacheEnabled={false}`**：每次重建都从零解析 HTML/图片，加重频闪观感。修复第一节的重载问题后可改为启用缓存。
2. **图片 base64 内联**：每章加载时把所有图片同步转 base64 嵌入 HTML，图多的章节（配方书常见）加载明显变慢且内存翻倍。方案：优先用 `baseUrl` + 相对路径让 WebView 直接读本地文件（代码已支持 baseUrl，可将 base64 内联降级为 fallback）。
3. **翻页动画不跟手**：多页章节滑动时外层容器不动（只有单页章节有位移动画），观感是"内容瞬移"。方案：翻页时在 WebView 内部用 CSS `scroll-behavior: smooth` 或 transform 过渡做 180ms 滑动动画，接近 Apple 图书的滑动翻页（完整的仿真卷页动画成本高，建议先做滑动式）。
4. **相邻章节预载**：Apple 图书跨章翻页无缝，是因为相邻资源预载。我们可在空闲时预读下一章 HTML 到内存，跨章翻页从"loading 转圈"变为即时切换。

---

## 六、其他潜在 bug（审查中顺带发现）

1. `goNextPage/goPrevPage` 更新了 `currentPage` 但未同步 `currentPageInChapter`，两个状态语义重叠且可能不一致，建议合并为一个。
2. `progressPanResponder` 依赖 `[book]`，30 秒一次的 book 引用变化会不断重建 PanResponder（修复第一节后自然消失）。
3. Web 平台渲染路径把完整 `<!DOCTYPE html>` 文档塞进 `dangerouslySetInnerHTML` 的 `<div>`，属非法 DOM 嵌套；且 Web 端无翻页模式、无点击唤出工具栏。若 Web/Mac Catalyst 是重要场景，建议 Web 端改为 iframe(srcdoc) 渲染并同样支持分页。
4. 高亮注入用固定 700ms 延时赌 WebView 加载完成，慢设备上会失效；应改为 WebView `onLoadEnd` 事件驱动。
5. 阅读位置恢复只恢复了章节，章内页码（`lastPage`）在章节重新分页后未恢复到对应页。

---

## 七、建议的实施顺序（等你确认）

| 批次 | 内容 | 预期效果 | 风险 |
|------|------|---------|------|
| **P0（bug 修复）** | 频闪根因修复（effect 依赖）+ 进度条拖动防抖 + 书签持久化 | 彻底消除自动频闪与拖动闪烁 | 低 |
| **P1（Mac 翻页）** | 点击边缘翻页热区 + 底部按钮翻页语义 + 键盘方向键 | Mac/iPad/iPhone 全都能可靠翻页 | 低 |
| **P2（双页+排版）** | 宽屏自动双页、行长上限、页脚页码重构、顶栏信息优化 | 接近 Apple 图书的宽屏阅读体验 | 中 |
| **P3（流畅度）** | 滑动翻页动画、相邻章预载、图片加载优化、锚点跳转、高亮时机 | 翻页跟手、跨章无缝 | 中 |

P0+P1 改动集中、风险低，可以一次完成并出一个 TestFlight 构建验证；P2/P3 涉及布局重构，建议在 P0/P1 验证通过后再做。

> 请告诉我从哪一批开始，或者是否要调整方案细节（例如双页触发宽度、翻页动画样式、是否需要仿真卷页效果等）。
